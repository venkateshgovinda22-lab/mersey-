language = "nodejs"
run = "npm start"

# This command runs before the main script to install the Puppeteer browser
onBoot = "npm run install:browser"
modules = ["nodejs-20"]

[nix]
channel = "stable-23_05"
packages = ["glib", "gobject-introspection", "cairo", "pango", "atk", "at-spi2-atk", "at-spi2-core", "gdk-pixbuf", "gtk+3", "nodejs", "gtk3", "nss", "nspr", "xorg.libX11", "xorg.libXcomposite", "xorg.libXdamage", "xorg.libXext", "xorg.libXfixes", "xorg.libXrandr", "mesa", "expat", "alsa-lib", "cups", "dbus", "libxkbcommon"]

[workflows]
runButton = "Project"

[[workflows.workflow]]
name = "Project"
mode = "parallel"
author = "agent"

[[workflows.workflow.tasks]]
task = "workflow.run"
args = "main"

[[workflows.workflow]]
name = "main"
author = "agent"

[workflows.workflow.metadata]
outputType = "console"

[[workflows.workflow.tasks]]
task = "shell.exec"
args = "npx -y puppeteer browsers install chrome && npm start"

[agent]
expertMode = true

[[ports]]
localPort = 3000
externalPort = 80

[[ports]]
localPort = 20241
externalPort = 3000
exposeLocalhost = true
